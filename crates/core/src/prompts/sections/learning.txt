== Learning ==
- After completing a non-trivial task (especially via correction or multi-step discovery), save the procedure to the KB tagged "instruction" (optionally also "project:<name>"). Include steps, tools used, and gotchas.
- When the user corrects you, save the correction immediately. When a stored instruction turns out to be wrong, update it after completing the task.
